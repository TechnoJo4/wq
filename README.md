# wq

wq (pronounced woke, but you can call it work-queue if you hate fun) is a cli issue tracker for agents

200 lines of typescript (code more readable than atw c!)

install: `deno install --global --allow-all --config deno.json --name wq wq`
